apiVersion: v1
kind: Service
metadata:
  name: vreapi-svc
  labels:
    app: vreapi-svc
spec:
  ports:
    type: NodePort
    - port: 8000
      protocol: TCP
      targetPort: 8000
      nodePort: 30001
  selector:
    app: vreapi
  type: ClusterIP