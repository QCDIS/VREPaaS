apiVersion: apps/v1
kind: Deployment
metadata:
  name: vreapi-deployment
  labels:
    app: vreapi
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vreapi
  template:
    metadata:
      labels:
        app: vreapi
    spec:
      containers:
        - name: vreapi
          image: qcdis/vreapi
          imagePullPolicy: Never
          ports:
          - containerPort: 8000
            protocol: TCP
          env:
            - name: SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: django-secrets
                  key: secret-key

            - name: DJANGO_SUPERUSER_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: django-secrets
                  key: superuser-password

            - name: DJANGO_SUPERUSER_EMAIL
              value: riccardo.bianchi@lifewatch.eu

            - name: DJANGO_SUPERUSER_USERNAME
              value: riccardo

            - name: DJANGO_SETTINGS_MODULE
              value: vreapis.settings.development

---

apiVersion: v1
kind: Service
metadata:
  name: vreapi-svc
  labels:
    app: vreapi-svc
spec:
  ports:
    - port: 8000
      protocol: TCP
      targetPort: 8000
  selector:
    app: vreapi
  type: ClusterIP

---

apiVersion: v1
kind: Secret
metadata:
  name: django-secrets
type: Opaque
data:
  secret-key:
  superuser-password:
  keycloak_server_url:
  keycloak_realm:
  keycloak_client_secret_key:
  keycloak_client_id: